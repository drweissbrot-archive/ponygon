footer {
	display flex
	align-items center
	justify-content center
	transition opacity .5s ease

	&:not(:active):not(:hover) {
		opacity .4
	}

	> * {
		display flex
		align-items center

		padding 2rem 4rem
	}

	img {
		height 1.5em
		margin-right 1ch
	}
}
